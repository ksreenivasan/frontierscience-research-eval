# FrontierScience-Research one-question smoke — 2026-08-25

**Directional plumbing result only; not comparable to the official 30-trial-per-question protocol.**

## Condition

- Public FrontierScience-Research gold set, pinned revision `25ed67db7da8f4591484e764008ff585544f5a30`.
- One chemistry task: source row 21, task group `e2aee1ae-6baa-4d30-a409-e0fc9c90f1fb`, deterministic high-length stratum.
- One authoritative answer per requested model; direct single-turn prompt; no tools, browsing, search, files, code execution, or agent loop.
- Uniform 32,768-token output cap.
- GPT-5.6 Sol: `reasoning.effort=high`.
- Claude Opus 5: adaptive thinking with `output_config.effort=high`.
- Gemini 3.7 Flash: `thinkingLevel=HIGH`.
- Qwen3.5-397B-A17B: `reasoning.enabled=true`, thinking sampling `temperature=0.6`, `top_p=0.95`, `top_k=20`; pinned Alibaba OpenRouter endpoint with provider fallbacks disabled.
- Judge: `gpt-5-2025-08-07`, `reasoning.effort=high`, published Research rubric prompt, pass threshold `>=7/10`.

## Authoritative results

| Model | Outcome | Rubric score | Pass | Generation cost | Judge cost |
|---|---|---:|---:|---:|---:|
| `gpt-5.6-sol` | completed | 2.5/10 | 0/1 | $0.2503 | $0.0743 |
| `claude-opus-5` | provider refusal | 0.0/10 | 0/1 | $0.0053 | $0.0331 |
| `gemini-3.7-flash` | completed | 5.0/10 | 0/1 | $0.0237 | $0.0540 |
| `qwen3.5-397b-a17b` | completed via Alibaba | 1.0/10 | 0/1 | $0.0153 | $0.0699 |

Authoritative calls recorded **$0.5259**. Preserved failed plumbing attempts add at least **$0.0238**, for a known lower bound of **$0.5497**. One early Claude refusal and one early judge-format attempt predated raw-response preservation, so their exact additional spend is unavailable; this is confined to the smoke and disclosed rather than estimated.

## Review

- Four authoritative answer artifacts and four authoritative judgments are present.
- The three non-refusal generations completed without truncation. Claude returned an explicit provider refusal with zero output tokens; the runner now records refusals as model outcomes rather than infrastructure failures.
- All four authoritative judge outputs were manually inspected. They apply the ten rubric items coherently, total to the stored scores, and agree with the `>=7` binary pass values.
- The judge returned a bare numeric `0` on one non-authoritative formatting retry. The parser now accepts a bare numeric only when the entire visible response is an unambiguous value in `[0,10]`; malformed and out-of-range outputs still fail.
- Failed generation and judgment responses are now preserved before validation. No provider key value appears in source, reports, manifests, or raw smoke artifacts.
- Offline container checks passed with a read-only root filesystem, no network for tests, all Linux capabilities dropped, `no-new-privileges`, bounded resources, no Docker socket, no host networking, read-only dataset/key mounts, and a single scoped writable artifact mount. No evaluation containers remain running.

## Disposition

The concrete smoke plumbing defects were fixed narrowly. The current runner is ready for the predeclared 9-question × four-model, one-answer-per-task pilot, but that pilot has **not** been started pending smoke review.
